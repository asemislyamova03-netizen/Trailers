# ТЗ: Trailers — продажи, производство, склад, VIN и роли

Дата: 2026-06-05
Обновлён: 2026-06-05
Статус: v2 / approved
Проект: Trailers Flask live/reference
Основа для implementation plan.
Кодить по этому документу нельзя без отдельного implementation plan.

---

## 1. Цель

Привести бизнес-процессы Trailers к понятной логике для продажи прицепов:

```text
Клиентский заказ
→ позиция заказа
→ источник прицепа
→ производство / наличие / перемещение
→ VIN
→ готовность к отгрузке
→ отгрузка
```

Система должна поддерживать два типа производственных потребностей:

```text
1. Производство под заказ клиента.
2. Производство на пополнение склада.
```

Менеджер управляет продажей, источником, резервом и подтверждением VIN. Производство принимает заявки в работу и выпускает прицепы. Логист вносит VIN. Подтверждение нанесения VIN делает менеджер/director/admin.

---

## 2. Основные сущности

### 2.1. Заказ клиента

Заказ клиента — коммерческий документ продажи.

В заказе должны быть:

- номер заказа;
- дата заказа;
- клиент;
- склад продажи;
- позиции заказа;
- сумма;
- статусы заказа;
- договор / оплата / отгрузка.

Заказ может содержать одну или несколько позиций.

### 2.2. Позиция заказа

Позиция заказа — конкретный прицеп или конфигурация прицепа.

Позиция должна хранить:

- прицеп / модель;
- артикул;
- конфигурацию;
- VIN, если уже известен;
- цену / сумму;
- источник позиции;
- склад-источник;
- склад продажи / склад назначения;
- связь с производственной потребностью, если есть;
- связь с перемещением, если есть;
- резерв, если есть;
- статус исполнения позиции.

### 2.3. Источник позиции заказа

У каждой позиции должен быть один понятный источник:

- из наличия на складе продажи;
- из наличия на другом складе через перемещение;
- из производства под клиента;
- из производства на склад с последующим резервом.

Требование:

```text
Источник должен редактироваться для любой позиции заказа, пока позиция не отгружена.
```

Ограничение:

```text
После физического выпуска/подтверждения VIN изменение источника должно быть ограничено бизнес-правилами, чтобы не потерять связь с фактическим прицепом.
```

### 2.4. Производственная потребность

Производственная потребность — заявка в производство.

Типы:

```text
client_order — под заказ клиента
stock_replenishment — на пополнение склада
```

Для `client_order` обязательно хранить связь:

- номер заказа клиента;
- позиция заказа;
- клиент;
- склад продажи;
- целевой склад.

Для `stock_replenishment` хранить:

- целевой склад;
- модель / артикул;
- конфигурация;
- количество;
- инициатор.

Ключевое требование:

```text
В производстве нельзя показывать только номер потребности.
Для производства нужно показывать номер клиентского заказа, если потребность создана под клиента.
```

Внутренний номер потребности может оставаться, но в интерфейсе он не должен быть главным идентификатором.

### 2.5. Резерв

Резерв — связь конкретного прицепа или будущего прицепа с клиентским заказом.

Типы резервов:

- резерв складского прицепа;
- резерв прицепа в производстве;
- резерв будущего выпуска под клиента.

Менеджер должен иметь право:

- снять резерв с прицепа;
- снять резерв с прицепа в производстве;
- снять резерв, если клиент отказался;
- перепривязать заказ к другому источнику.

Если прицеп уже в производстве и стал не нужен клиенту:

```text
производство не обязательно отменяется;
резерв снимается;
производственная потребность переводится в производство на склад / свободный складской выпуск.
```

### 2.6. Перемещение

Если нужный прицеп есть на другом складе, менеджер должен создавать не производство, а заявку на перемещение.

Заявка на перемещение должна связывать:

- позицию заказа;
- склад-источник;
- склад назначения;
- прицеп / VIN, если известен;
- статус перемещения;
- ответственного.

Базовые статусы перемещения:

- требуется перемещение;
- заявка на перемещение создана;
- в перемещении;
- прибыло на склад продажи;
- готов к отгрузке;
- отменено.

---

## 3. Роли и права

### 3.1. Менеджер

Менеджер может:

- создавать заказ клиента;
- редактировать заказ клиента;
- создавать позицию по конфигуратору;
- добавлять позицию из наличия;
- выбирать источник позиции;
- отправлять позицию в производство;
- создавать заявку на перемещение;
- создавать производственную потребность под клиента;
- создавать производственную потребность на пополнение склада;
- редактировать комплектацию позиции;
- снимать резерв;
- отменять заявку на производство, если она ещё не принята производством;
- снимать бронь с прицепа, даже если он уже в производстве;
- подтверждать нанесение VIN.

Менеджер не должен:

- выпускать прицеп из производства;
- менять производственный статус "в работе" / "выпущен";
- выполнять deploy/system actions.

### 3.2. Производство

Производство может:

- видеть список заявок в производство;
- видеть артикул / модель / конфигурацию;
- видеть, под клиента это или на склад;
- видеть номер заказа клиента, если заявка под клиента;
- принять заявку в работу;
- указать плановую дату выпуска;
- изменить статус "в производстве";
- отметить "готово / выпущено".

Производство не должно:

- создавать клиентский заказ;
- снимать резерв;
- подтверждать VIN как менеджер;
- менять коммерческие условия заказа;
- менять цену;
- менять клиента.

### 3.3. Логист

Логист может:

- набивать VIN на прицепы;
- вносить VIN в систему;
- видеть VIN-реестр;
- видеть статус подтверждения VIN;
- видеть подсказку, если подтверждение VIN недоступно.

Логист не должен:

- подтверждать нанесение VIN;
- создавать заказ клиента;
- отправлять в производство;
- снимать резерв;
- менять комплектацию заказа.

### 3.4. Директор / Admin

Директор и admin могут:

- видеть все заказы;
- видеть все склады;
- видеть все производственные заявки;
- подтверждать VIN;
- снимать резерв;
- отменять заявки;
- исправлять ошибки менеджеров.

Admin должен иметь доступ к тем же действиям, что и manager/director, если route уже допускает admin.

---

## 4. Правила комплектации

### 4.1. Что можно менять

Комплектацию прицепа можно менять на любой стадии, кроме ограничений ниже.

Можно менять:

- цвет;
- дополнительные опции;
- комплектующие;
- надстройки;
- оборудование;
- коммерческое описание;
- цену, если это разрешено ролью.

### 4.2. Что нельзя менять

Нельзя менять:

- размер рамы;
- количество осей.

Эти параметры считаются базовой производственной конструкцией.

### 4.3. Если прицеп уже в производстве

Если прицеп уже принят в производство:

- изменение комплектации должно быть явно видно производству;
- должна быть отметка "изменение комплектации";
- история изменений должна сохраняться;
- производство должно видеть актуальную версию комплектации.

Если изменение невозможно физически, производство должно иметь возможность вернуть заявку менеджеру с комментарием.

---

## 5. Основные бизнес-процессы

### 5.1. Сценарий A: продажа прицепа из наличия на складе продажи

1. Менеджер создаёт заказ.
2. Выбирает клиента.
3. Выбирает склад продажи.
4. Добавляет позицию из наличия.
5. Система показывает доступные прицепы на этом складе.
6. Менеджер выбирает прицеп.
7. Система создаёт резерв под заказ.
8. Позиция получает статус: "В наличии на складе продажи".
9. После оплаты/договора позиция становится: "Готов к отгрузке".
10. После отгрузки заказ получает статус: "Отгружен".

### 5.2. Сценарий B: продажа прицепа, который есть на другом складе

1. Менеджер создаёт заказ.
2. Добавляет позицию из наличия.
3. Система показывает, что нужный прицеп есть на другом складе.
4. Менеджер выбирает этот прицеп.
5. Система создаёт резерв под заказ.
6. Система предлагает создать заявку на перемещение.
7. Менеджер создаёт перемещение.
8. Позиция получает статус: "Требуется перемещение".
9. После отправки: "В перемещении".
10. После прибытия: "Готов к отгрузке".

### 5.3. Сценарий C: продажа прицепа под производство

1. Менеджер создаёт заказ.
2. Добавляет позицию через конфигуратор.
3. Система создаёт позицию заказа без VIN.
4. Менеджер нажимает "В производство".
5. Система создаёт производственную потребность типа `client_order`.
6. Производственная потребность связывается с заказом клиента, позицией заказа, клиентом, складом продажи и целевым складом.
7. Позиция получает статус: "Заказан в производство".
8. Производство принимает заявку.
9. Позиция получает статус: "В производстве".
10. Производство указывает плановую дату выпуска.
11. После выпуска позиция получает статус: "Готов к перемещению" или "Готов к отгрузке" — в зависимости от склада.
12. Логист наносит/вносит VIN.
13. Менеджер подтверждает VIN.
14. Позиция становится готовой к отгрузке.

### 5.4. Сценарий D: производство на пополнение склада

1. Менеджер создаёт производственную потребность не из клиентского заказа, а на склад.
2. Указывает склад, модель/артикул, конфигурацию и количество.
3. Производство принимает заявку.
4. Производство выпускает прицеп.
5. Логист вносит VIN.
6. Менеджер/директор подтверждает VIN.
7. Прицеп становится свободным складским остатком.

Такие заявки должны отображаться отдельно от клиентских.

### 5.5. Сценарий E: клиент отказался, прицеп уже в производстве

1. Позиция заказа связана с производственной потребностью.
2. Производство уже приняло заявку.
3. Клиент отказался или менеджер решил заменить источник.
4. Менеджер снимает резерв с позиции.
5. Связь с клиентским заказом убирается или переводится в статус "резерв снят".
6. Производственная потребность не удаляется.
7. Потребность становится "производство на склад".
8. Готовый прицеп поступает на целевой склад как свободный остаток.

Важно:

```text
Менеджеру не надо отменять производство, если оно уже началось.
Он должен иметь право снять бронь.
```

### 5.6. Сценарий F: заявка в производство ещё не принята

Если заявка ещё не принята производством:

- менеджер может отменить заявку на производство;
- позиция заказа возвращается в статус "требуется источник";
- менеджер может выбрать наличие, перемещение или новую конфигурацию.

---

## 6. Статусы

### 6.1. Статусы заказа

В заказе сверху должны отображаться простые стадии в кружочках/чипах:

- Создан;
- Оплачен;
- Есть договор;
- Отгружен.

Требование к UI:

```text
компактная горизонтальная строка статусов;
активная стадия подсвечена;
неактивная стадия серая.
```

### 6.2. Статусы позиции заказа

Для каждой позиции заказа должны быть видны статусы:

- В наличии на складе продажи;
- Требуется перемещение;
- Заказан в производство;
- В производстве — плановая дата выпуска;
- Готов к перемещению;
- Готов к отгрузке;
- Отгружен;
- Резерв снят;
- Отменён.

Статусы должны быть видны рядом с позицией, а не спрятаны во вкладках.

### 6.3. Статусы производственной потребности

- Создана;
- Отправлена в производство;
- Принята в работу;
- В производстве;
- Выпущена;
- Переведена на склад;
- Отменена.

### 6.4. Статусы VIN

- VIN не указан;
- VIN внесён логистом;
- VIN подтверждён менеджером;
- VIN отклонён / требует исправления.

---

## 7. Экран создания заказа

### 7.1. Текущая проблема

Сейчас при создании заказа интерфейс выглядит как вертикальные столбцы, справа большой скролл. Это неудобно, особенно для мобильной версии.

### 7.2. Новый принцип

Экран создания заказа должен быть логически разделён:

```text
Верх: данные клиента и заказа
Низ: выбор позиции
```

### 7.3. Верхняя часть

Верхняя часть:

- клиент;
- телефон;
- склад продажи;
- дата;
- менеджер;
- комментарий.

### 7.4. Нижняя часть

Нижняя часть должна иметь переключатель:

- Конфигуратор;
- Наличие.

#### Конфигуратор

Используется, если прицеп создаётся под заказ.

После сохранения заказа позиция получает источник:

```text
требуется производство
```

#### Наличие

Используется, если прицеп выбирается со склада.

Система должна показывать:

- на складе продажи;
- на других складах;
- зарезервировано;
- свободно.

Если выбран прицеп с другого склада:

```text
создаётся/предлагается заявка на перемещение
```

---

## 8. Карточка заказа

### 8.1. Новый layout

Карточка заказа должна быть компактной.

Верх:

- номер заказа;
- дата;
- клиент;
- телефон;
- склад продажи;
- сумма.

Справа или в верхней строке:

- основные действия.

Ниже:

- стадии заказа в кружочках: Создан → Оплачен → Есть договор → Отгружен.

Дальше:

- позиции заказа.

Не нужны большие информационные плитки, которые занимают половину страницы и не дают смысла.

### 8.2. Таблица / карточки позиций заказа

Колонки для desktop:

- Прицеп;
- Артикул;
- VIN;
- Сумма;
- Источник;
- Склад;
- Статус позиции;
- Действия.

Источник должен быть одним из:

- Из наличия;
- Требуется перемещение;
- Из производства;
- На склад.

Для позиции должны быть видны действия:

- Изменить комплектацию;
- Изменить источник;
- В производство;
- Создать перемещение;
- Снять резерв;
- Отменить заявку;
- Подтвердить VIN;
- Готов к отгрузке.

Кнопки должны быть контекстные: показывать только то, что возможно на текущей стадии и по роли.

---

## 9. Кнопки и доступность действий

### 9.1. Проблема

Сейчас важные кнопки спрятаны:

- Подтвердить VIN;
- Отдать в производство.

### 9.2. Требование

Кнопки должны быть видны:

- в карточке заказа рядом с позицией;
- в производственном списке;
- в VIN registry.

Для мобильной версии:

```text
основные действия должны быть в верхней sticky-панели или прямо в карточке позиции
```

### 9.3. Приоритетные кнопки

Для менеджера:

- В производство;
- Создать перемещение;
- Снять резерв;
- Отменить заявку;
- Подтвердить VIN;
- Изменить комплектацию;
- Изменить источник.

Для производства:

- Принять в работу;
- Указать плановую дату;
- Выпущено.

Для логиста:

- Внести VIN.

---

## 10. Возврат после действий

### 10.1. Текущая проблема

После действий типа:

- создать ТТН;
- договор выдан;

система перекидывает на первый лист или в начало.

### 10.2. Требование

После любого POST-действия пользователь должен возвращаться туда, откуда пришёл:

- тот же заказ;
- та же вкладка;
- та же позиция;
- тот же scroll/anchor, если возможно.

Минимальное решение:

- `return_to` / `next URL`;
- anchor на позицию заказа;
- сохранение активной вкладки.

---

## 11. Экран производства

### 11.1. Основная идея

Производству не нужна перегруженная CRM-карточка.

Производству нужен простой список:

- артикул;
- модель/прицеп;
- количество;
- тип заявки;
- под кого;
- номер заказа;
- склад назначения;
- плановая дата;
- статус.

### 11.2. Группировка

Список производства должен быть разделён:

- Под клиента;
- На склад.

#### Под клиента

Показывать:

- номер заказа клиента;
- клиент;
- артикул;
- модель;
- склад продажи;
- плановая дата;
- статус.

#### На склад

Показывать:

- склад назначения;
- артикул;
- модель;
- количество;
- плановая дата;
- статус.

### 11.3. Проваливание в детали

В списке не надо показывать все характеристики.

По клику открывается детальная карточка:

- полная комплектация;
- история изменений;
- комментарии менеджера;
- производственные комментарии;
- VIN, если уже назначен;
- связанный заказ / резерв.

---

## 12. VIN-процесс

### 12.1. Логист

Логист:

- набивает VIN;
- вносит VIN в систему.

После этого VIN получает статус:

```text
VIN внесён
```

### 12.2. Менеджер

Менеджер:

- проверяет VIN;
- подтверждает нанесение VIN.

После этого VIN получает статус:

```text
VIN подтверждён
```

### 12.3. Права

```text
logistics: внести VIN
manager/director/admin: подтвердить VIN
```

Если пользователь не имеет права подтверждать VIN:

```text
он видит статус и подсказку, но не кнопку
```

---

## 13. Поиск и идентификация

### 13.1. Текущая проблема

Когда прицеп в производстве, у него указан номер потребности, а не номер заказа клиента. Из-за этого его трудно найти.

### 13.2. Требование

Во всех производственных списках и карточках показывать:

- номер заказа клиента, если заявка под клиента;
- номер потребности как технический номер;
- клиента;
- артикул;
- модель;
- VIN, если есть;
- склад.

Поиск должен работать по:

- номеру заказа;
- номеру потребности;
- клиенту;
- артикулу;
- VIN;
- складу.

---

## 14. Резервы

### 14.1. Типы резервов

- резерв складского прицепа;
- резерв прицепа в производстве;
- резерв будущего выпуска под клиента.

### 14.2. Снятие резерва

Менеджер должен иметь право:

```text
снять резерв с любого прицепа, если это не нарушает уже закрытую отгрузку
```

Если прицеп ещё не отгружен:

```text
резерв можно снять
```

Если прицеп уже отгружен:

```text
резерв не снимается, потому что сделка закрыта
```

### 14.3. Последствие снятия резерва

Если резерв снят со складского прицепа:

```text
прицеп снова становится свободным остатком
```

Если резерв снят с прицепа в производстве:

```text
прицеп продолжает производиться, но уже как свободный складской выпуск
```

Если резерв снят с ещё не принятой производственной заявки:

```text
заявку можно отменить;
позиция заказа возвращается к выбору источника.
```

---

## 15. Отмена заявки на производство

### 15.1. Когда можно отменить

Менеджер может отменить заявку, если она:

- создана;
- отправлена;
- но ещё не принята производством.

### 15.2. Когда нельзя отменить напрямую

Если заявка уже:

- принята в работу;
- в производстве;
- выпущена;

тогда менеджер не отменяет производство, а:

- снимает резерв;
- переводит выпуск на склад;
- выбирает другой источник для клиентского заказа.

---

## 16. Редактирование строки прицепа в заказе

### 16.1. Текущее ограничение

Сейчас в заказе можно:

- добавить по конфигуратору;
- добавить из наличия.

Но после этого не хватает нормального редактирования строки.

### 16.2. Требование

В сохранённом заказе должна быть команда:

```text
Редактировать позицию
```

Она должна позволять:

- изменить комплектацию;
- изменить источник;
- перевыбрать складской прицеп;
- создать / отменить перемещение;
- отправить в производство;
- снять резерв.

Запрещено менять:

- размер рамы;
- количество осей.

Если эти параметры нужно изменить, система должна требовать:

```text
создать новую позицию;
отменить / закрыть старую позицию.
```

---

## 17. Мобильная версия

Требования:

- без широких таблиц, которые ломают экран;
- позиции заказа должны быть карточками на мобильном;
- основные действия видны сразу;
- статусы — компактные чипы;
- клиент / номер / дата — сверху;
- вкладки не должны прятать критические кнопки.

Для карточки позиции на мобильном:

- Прицеп / артикул;
- VIN;
- Источник;
- Статус;
- Сумма;
- Основная кнопка действия;
- Дополнительные действия в меню.

---

## 18. Минимальная матрица прав

| Действие | Manager | Production | Logistics | Director | Admin |
|---|---:|---:|---:|---:|---:|
| Создать заказ | Да | Нет | Нет | Да | Да |
| Редактировать заказ | Да | Нет | Нет | Да | Да |
| Добавить позицию из конфигуратора | Да | Нет | Нет | Да | Да |
| Добавить позицию из наличия | Да | Нет | Нет | Да | Да |
| Изменить комплектацию | Да | Нет | Нет | Да | Да |
| Изменить размер рамы | Нет | Нет | Нет | Нет | Нет |
| Изменить количество осей | Нет | Нет | Нет | Нет | Нет |
| Создать заявку в производство | Да | Нет | Нет | Да | Да |
| Создать заявку на перемещение | Да | Нет | Нет | Да | Да |
| Принять в производство | Нет | Да | Нет | Да | Да |
| Выпустить из производства | Нет | Да | Нет | Да | Да |
| Внести VIN | Нет | Нет | Да | Да | Да |
| Подтвердить VIN | Да | Нет | Нет | Да | Да |
| Снять резерв | Да | Нет | Нет | Да | Да |
| Отменить заявку до принятия | Да | Нет | Нет | Да | Да |
| Снять бронь с прицепа в производстве | Да | Нет | Нет | Да | Да |
| Отгрузить | Да | Нет | Возможно | Да | Да |

---

## 19. Приоритет реализации

### Этап 1. Зафиксировать процессы и права

Без кода:

- утвердить статусы;
- утвердить роли;
- утвердить правила резервов;
- утвердить правила производства;
- утвердить правила VIN.

Результат:

```text
docs/ai/live-support/2026-06-05-trailers-sales-production-workflow-tz.md
```

### Этап 2. Исправить видимость критичных кнопок

Статус: частично выполнен (2026-06-05).

Выполнено:

- `039c80a` — кнопка "В производство" видна в заказе рядом с позицией (`views.py`, `templates/order_detail.html`);
- `071ab28` — кнопка "Подтвердить VIN" исправлена в VIN registry и карточке заказа (`templates/vin_registry_list.html`, `templates/vin_registry_detail.html`);
- `971d1b6` — admin видит кнопку "Подтвердить VIN" в VIN registry (P1 fix, `templates/vin_registry_list.html`).

Остаётся:

- кнопка "Снять резерв" видна менеджеру;
- кнопка "Создать перемещение" видна при наличии на другом складе.

### Этап 3. Привести карточку заказа к новой структуре

P1/P2:

- верх: номер, дата, клиент;
- ниже: статусы заказа;
- ниже: позиции;
- убрать/сжать бессмысленные плитки;
- показать источник позиции и статус исполнения.

### Этап 4. Редактирование позиции заказа

P1:

- изменить комплектацию;
- изменить источник;
- запретить менять размер рамы и количество осей;
- сохранять историю изменений.

### Этап 5. Производственный список

P1/P2:

- разделить "Под клиента" и "На склад";
- показывать номер заказа клиента;
- показывать технический номер потребности вторым уровнем;
- добавить поиск по заказу / VIN / артикулу / клиенту.

### Этап 6. Резервы и снятие брони

P1:

- снять резерв со складского прицепа;
- снять резерв с прицепа в производстве;
- перевести производство под клиента в производство на склад;
- отменить только не принятую заявку.

### Этап 7. Возврат после действий

P2:

- после "договор выдан";
- после "создать ТТН";
- после POST-действий;
- возвращать пользователя в то же место.

---

## 20. Acceptance Criteria

### 20.1. Заказ

- менеджер создаёт заказ без вертикального перегруженного скролла;
- клиентские данные сверху;
- конфигуратор / наличие снизу;
- после сохранения видна компактная карточка заказа;
- статусы заказа видны сверху;
- позиции заказа читаются без переходов по вкладкам.

### 20.2. Производство

- производство видит список простых задач;
- задачи разделены "под клиента" и "на склад";
- под клиентом виден номер заказа;
- по клику открываются детали комплектации;
- производство не управляет резервами и коммерцией.

### 20.3. VIN

- логист вносит VIN;
- менеджер подтверждает VIN;
- admin тоже видит кнопку подтверждения; (исправлено `971d1b6`, 2026-06-05)
- logistics не видит кнопку подтверждения;
- VIN-статус понятен в заказе и VIN registry.

### 20.4. Резервы

- менеджер может снять резерв;
- если производство не началось — заявку можно отменить;
- если производство началось — резерв снимается, выпуск идёт на склад;
- свободные прицепы возвращаются в наличие.

### 20.5. Перемещение

- если прицеп есть на другом складе, создаётся перемещение;
- позиция заказа показывает "требуется перемещение";
- после прибытия статус меняется на "готов к отгрузке".

### 20.6. Навигация

- после POST-действий пользователь возвращается в ту же карточку / раздел;
- важные кнопки не спрятаны во вкладках;
- мобильная версия не требует горизонтальных таблиц.

---

## 21. Что не делать в первом кодовом этапе

Не делать сразу:

- полный rewrite заказов;
- новую ERP-архитектуру;
- перенос в Flexity;
- глубокие миграции без плана;
- изменение deploy/Nginx/systemd;
- переписывание всех templates;
- изменение прав без матрицы.

Первый кодовый шаг должен быть маленький:

1. задокументировать ТЗ;
2. найти текущие модели/routes/templates;
3. составить implementation plan;
4. выбрать один P1 сценарий;
5. исправить максимум 1–3 файла;
6. проверить diff;
7. остановиться перед deploy.

---

## 22. Рекомендуемый первый implementation plan

Начинать не с производства целиком, а со связки:

```text
order_detail → production request → production list display
```

Первый маленький кодовый шаг:

```text
Показывать в производстве номер заказа клиента рядом с производственной потребностью.
Разделить отображение заявок:
- под клиента;
- на склад.
Не менять бизнес-логику.
Только отображение и поиск.
```

Почему это лучше первым:

- сразу решает проблему "невозможно найти прицеп в производстве";
- низкий риск;
- не требует ломать заказы;
- помогает менеджеру и производству.

Второй шаг:

```text
редактирование источника позиции заказа
```

Третий шаг:

```text
снятие резерва / перевод производства под клиента в производство на склад
```

Четвёртый шаг:

```text
перестройка UI карточки заказа
```

---

## 23. P0: Обязательная складская привязка

### Проблема

После удаления производственного склада из общих настроек появилась критичная проблема:

```text
заказы создаются без склада;
прицепы создаются без склада;
производственные заявки теряют понятный склад-источник / склад назначения;
невозможно корректно понять, где физически находится прицеп.
```

Это P0/P1 дефект, потому что складская привязка влияет на:

```text
резервы;
наличие;
производство;
перемещение;
VIN;
готовность к отгрузке;
поиск прицепов;
отчёты по складам.
```

### Бизнес-правило

Нельзя создавать заказ, позицию заказа, складской прицеп, производственную заявку или перемещение без явной складской привязки.

Минимально обязательные поля:

```text
order.sales_warehouse_id
order_item.source_warehouse_id, если выбран складской прицеп
order_item.target_warehouse_id, если позиция должна поступить на склад продажи
trailer.warehouse_id, если прицеп физически существует
production_request.target_warehouse_id
production_request.production_warehouse_id или production_location_id, если производство отделено от склада продажи
transfer.source_warehouse_id
transfer.target_warehouse_id
```

### Производственный склад

В системе должен быть понятный производственный контекст.

Варианты реализации:

1. Вернуть системную настройку:

```text
default_production_warehouse_id
```

2. Или ввести отдельное понятие:

```text
production_location_id
```

3. Или требовать выбор склада производства при отправке в производство.

Рекомендуемый минимальный вариант для live-проекта:

```text
вернуть / восстановить default_production_warehouse_id в настройках
и использовать его как fallback только для производственных заявок,
но не как замену складу продажи или складу назначения.
```

### Правило fallback

Если склад не задан:

```text
не создавать тихо заказ/прицеп без склада;
показать ошибку менеджеру;
подсветить поле склада;
записать понятное сообщение.
```

Запрещено:

```text
подставлять None;
создавать trailer без warehouse_id;
создавать production_request без target_warehouse_id;
создавать order без sales_warehouse_id.
```

### Data integrity check

Нужно добавить проверку существующих проблемных данных:

```text
заказы без sales_warehouse_id;
позиции заказа без source/target warehouse;
прицепы без warehouse_id;
производственные заявки без target_warehouse_id;
резервы без связанного склада.
```

Это не означает немедленную миграцию. Сначала нужен read-only audit.

### Acceptance criteria

```text
новый заказ невозможно сохранить без склада продажи;
прицеп невозможно создать как складской остаток без warehouse_id;
заявка в производство всегда имеет target_warehouse_id;
производственная заявка под клиента показывает склад продажи и целевой склад;
при выборе прицепа из другого склада система создаёт сценарий перемещения;
старые записи без склада найдены и вынесены в audit.
```

---

## 24. P1: Единая модель статусов заказа, позиции и прицепа

### Проблема

В списке заказов статусы не соответствуют статусам прицепов внутри заказа.

Выглядит так, будто:

```text
у заказа одно определение статуса;
у позиции заказа другое;
у прицепа третье;
в списке заказов показывается агрегат, который не совпадает с реальным состоянием позиций.
```

Из-за этого менеджеру непонятно:

```text
заказ оплачен или не оплачен;
прицеп готов или ещё в производстве;
нужно перемещение или нет;
VIN подтверждён или нет;
можно отгружать или нельзя.
```

### Принцип

Нужно разделить статусы на разные уровни, а не смешивать их в одно поле.

Уровни:

```text
1. Коммерческий статус заказа.
2. Исполнительный статус позиции заказа.
3. Физический статус прицепа.
4. Статус производственной заявки.
5. Статус VIN.
6. Статус перемещения.
```

### Коммерческий статус заказа

Отвечает на вопрос:

```text
что происходит с продажей?
```

Возможные значения:

```text
created
contract_prepared
contract_issued
paid
partially_paid
ready_to_ship
shipped
cancelled
```

UI-метки:

```text
Создан
Договор подготовлен
Договор выдан
Оплачен
Частично оплачен
Готов к отгрузке
Отгружен
Отменён
```

### Исполнительный статус позиции заказа

Отвечает на вопрос:

```text
откуда берётся конкретный прицеп и готов ли он?
```

Возможные значения:

```text
source_required
available_on_sales_warehouse
reserved_from_stock
transfer_required
transfer_in_progress
production_required
production_requested
in_production
produced
vin_required
vin_entered
vin_confirmed
ready_to_ship
shipped
reservation_removed
cancelled
```

UI-метки:

```text
Требуется источник
В наличии на складе продажи
Зарезервирован из наличия
Требуется перемещение
В перемещении
Требуется производство
Заказан в производство
В производстве
Выпущен
Требуется VIN
VIN внесён
VIN подтверждён
Готов к отгрузке
Отгружен
Резерв снят
Отменён
```

### Физический статус прицепа

Отвечает на вопрос:

```text
что физически происходит с единицей прицепа?
```

Возможные значения:

```text
planned
in_production
produced
in_stock
reserved
in_transfer
sold
shipped
cancelled
```

UI-метки:

```text
Запланирован
В производстве
Выпущен
На складе
В резерве
В перемещении
Продан
Отгружен
Отменён
```

### Статус производственной заявки

```text
draft
sent_to_production
accepted
in_progress
produced
transferred_to_stock
cancelled
```

UI-метки:

```text
Черновик
Отправлена в производство
Принята
В работе
Выпущена
Передана на склад
Отменена
```

### Статус VIN

```text
missing
entered_by_logistics
confirmed_by_manager
rejected
```

UI-метки:

```text
VIN не указан
VIN внесён логистом
VIN подтверждён менеджером
VIN отклонён
```

### Что показывать в списке заказов

В списке заказов нельзя показывать один мутный статус, который скрывает реальную ситуацию.

Минимально нужно показывать две колонки/метки:

```text
коммерческий статус заказа;
исполнение позиций.
```

Пример:

```text
Заказ №123
Коммерция: Оплачен
Исполнение: 1 в производстве, 1 готов к отгрузке
```

Или для одной позиции:

```text
Коммерция: Договор выдан
Исполнение: В производстве — план 12.06.2026
```

### Агрегация статуса исполнения заказа

Если нужен один агрегированный статус исполнения заказа, он должен вычисляться из позиций по приоритету.

Рекомендуемый порядок:

```text
cancelled — если заказ отменён;
shipped — если все позиции отгружены;
ready_to_ship — если все активные позиции готовы к отгрузке;
vin_required — если хотя бы одна позиция выпущена, но VIN не подтверждён;
in_production — если хотя бы одна позиция в производстве;
production_requested — если хотя бы одна позиция заказана в производство;
transfer_required / transfer_in_progress — если хотя бы одна позиция требует/ждёт перемещение;
reserved_from_stock — если все позиции зарезервированы из наличия;
source_required — если хотя бы одной позиции не выбран источник.
```

### Запрет

Нельзя определять статус заказа напрямую по одному полю прицепа.

Нельзя смешивать:

```text
оплату;
договор;
производство;
склад;
VIN;
отгрузку
```

в одно неочевидное поле.

### Acceptance criteria

```text
в списке заказов статус заказа не противоречит статусу прицепа внутри;
коммерческий статус виден отдельно от исполнения;
позиция заказа показывает свой источник и статус;
прицеп показывает физический статус;
производственная заявка показывает свой статус;
VIN показывает отдельный статус;
менеджер понимает, что именно мешает отгрузке.
```

---

## 25. Рекомендуемый порядок реализации с учётом разделов 23–24

### Шаг 1. Read-only audit складов

Найти:

```text
orders без sales_warehouse_id;
trailers без warehouse_id;
production_requests без target_warehouse_id;
order_items без source/target warehouse;
расхождения между order status и trailer/order item status.
```

Без изменений данных.

### Шаг 2. P0 guard для новых записей без склада

Запретить создание новых проблемных записей без склада:

```text
order без sales_warehouse_id;
trailer без warehouse_id;
production_request без target_warehouse_id.
```

### Шаг 3. Восстановить production warehouse setting или production location

Вернуть системное понятие производственного склада/локации.

### Шаг 4. Разделить отображение статусов

В списке заказов показывать:

```text
коммерческий статус;
исполнение позиций.
```

### Шаг 5. Синхронизировать карточку заказа

В карточке заказа показать:

```text
коммерческие стадии сверху;
у каждой позиции — source/status/VIN/warehouse;
не смешивать это в одну плитку.
```

### Шаг 6. Только после этого делать крупные UI-перестройки

```text
новый layout создания заказа;
редактирование позиции;
производственный список;
мобильная версия.
```
