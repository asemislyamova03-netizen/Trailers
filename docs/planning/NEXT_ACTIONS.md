# NEXT_ACTIONS.md

## Следующие действия

1. Проверить локально изменённый контур ролей: менеджер отдаёт потребности в производство, логист видит только VIN-реестр, производственные кнопки для менеджера неактивны.
2. Подтянуть последние изменения на сервер и перезапустить приложение после отдельного подтверждения.
3. На сервере запустить проверку синтаксиса через рабочий venv: `venv/bin/python -m py_compile views.py`.
4. Проверить меню логиста: вход должен вести в `Реестр VIN`, без блока потребностей/производства.
5. Проверить меню менеджера: в производственном блоке должны быть новые потребности и кнопка `Отдать в производство`.
6. Проверить, что менеджер может привязать VIN к выпущенной единице без VIN, а производственник по-прежнему выпускает изделие кнопкой `Выпущено 1 шт.`.
7. Проверить, что менеджер может отправить готовый прицеп со склада выпуска на свой склад кнопкой `Переместить на мой склад`, а логист не выполняет это действие.
8. Проверить сортировку реестра VIN у логиста: сверху должен быть самый большой порядковый номер `serial7`.
9. Проверить изменение комплектации у менеджера: смена борта, тента и опорного колеса без автоматической подстановки тента/каркаса 90.
10. Проверить производство и реестр VIN на смартфоне: карточки производства должны читаться без горизонтального скролла, а в VIN-реестре кнопка `Открыть` должна быть рядом с VIN.
11. Проверить в заказе `ORD-000043`: добавление второй другой конфигурации в производство создаёт отдельную строку заказа и `SupplyNeed` с `order_line_id`.
12. Проверить создание договора по заказу с разными строками: в `sales_contract_line` должны появиться строки, печатная спецификация должна вывести несколько позиций.
13. Подготовить безопасную интеграцию Kaspi: определить API заказов/платежей, место хранения токена в env/настройках и модель сопоставления Kaspi-заказа с `CustomerOrder`.
14. Проверить безопасное редактирование/удаление строк заказа до выдачи документов на сервере.
15. Проверить построчную выдачу документов и физическую отгрузку заказа по нескольким прицепам.
16. Проверить выбор шаблонов договоров по группе/ОТТС/модификации через экран `Шаблоны договоров`.
17. Спроектировать списание комплектующих и полуфабрикатов по спецификации при выпуске единицы прицепа.
18. Добавить мобильный интерфейс смен сотрудников: открыть смену, выбрать участок, закрыть смену с результатом или без результата.

## Ближайшая техническая задача

- Подготовить перенос/проверку проекта на хостинге организации: сделать backup `trailers.db` и проекта, развернуть код, перенести env-переменные, проверить доступ к `kaspi.kz`.
- Проверить на сервере новый раздел `Отчеты` под manager/director/logistics: директор должен видеть агрегированные графики и диаграммы, без списка заказов внизу.
- Проверить, что Excel-история продаж отражается в отчетах через `SalesContract`; при необходимости сделать отдельную нормализацию импортированных строк в договоры.
- Проверить фильтр направления в отчетах: `Легковые`, `Грузовые / спец`, `Без VIN / прочее`; аномальные договоры с высокой суммой должны быть видны предупреждением.
- После миграции проверить директорский отчет по филиалам/менеджерам: исторические договоры на 18,8 млн, 18 млн, 14 млн и 8,5 млн должны относиться к складу грузовых прицепов и Кальману Игорю.
- Проверить создание заказа задним числом: дата заказа должна сохраняться в `CustomerOrder.created_at`, а список заказов и отчеты должны использовать эту дату.
- Проверить импорт Kaspi-заказа уже на хостинге организации: задать `KASPI_SHOP_TOKEN`, открыть раздел `Kaspi`, импортировать заказ по коду или списком за период/состояние и убедиться, что появилась заявка и входящее сообщение в диалогах.

## Ближайшая бизнес-задача

- Определить хостинг организации, где будет проверяться Kaspi, и кто отвечает за доступы/домен/env-переменные.
- Подтвердить новую модель производства: участки, склады комплектующих/полуфабрикатов, сотрудники, смены и результаты месяца для директора.
- Уточнить правила для разных шаблонов договоров по группам/ОТТС/модификациям и будущие этапы оплат.
- Заполнить первые реальные правила `contract_template` для грузовых и легковых договоров.
- Уточнить сценарий Kaspi: это импорт заказов магазина, платежные ссылки/QR, статусы оплат или всё вместе.
- После проверки первого импорта Kaspi решить, нужен ли автоматический фоновый sync новых заказов или пока достаточно ручного импорта по номеру заказа.
- Спроектировать справочник соответствий `артикул Kaspi -> наша номенклатура`, потому что артикулы магазина могут отличаться от внутренних артикулов CRM.
- Спроектировать безопасный механизм замены ответственного лица по заказам/договорам и команду связки исторического договора с созданным задним числом заказом.

## Что нельзя забыть

- Не смешивать trailers с clinic app и flexity admin.
- Цены по конфигурации могут быть не заполнены и не должны блокировать заказ.
- Источник выбора параметров конфигуратора - матрица допустимости, а не справочник номенклатуры и не ценовые матрицы.
- Разные прицепы в одном заказе должны жить как разные строки заказа, а не как количество одной строки.
- Выдача документов и отгрузка должны проверяться по строкам заказа, иначе один VIN может ошибочно закрыть весь заказ.
- Удаление заказа должно оставаться безопасным: нельзя удалять заказ, если уже есть резервы, VIN, договор, оплаты, производство, перемещения, документы или отгрузка.
- Токен Kaspi нельзя коммитить в репозиторий; хранить только в переменных окружения или закрытых настройках сервера.
- Текущий сервер не получает ответ от `kaspi.kz`; проверку Kaspi продолжать на хостинге организации или другом IP.
- Kaspi и будущий WhatsApp должны попадать в общий контур `Lead`/`LeadMessage`, чтобы менеджер видел их в диалогах, а не в отдельном изолированном разделе.
- Отчеты для менеджера должны оставаться ограничены его складом/назначенными заказами.
- Для исторических отчетов источник выручки должен быть договор/продажа с датой и суммой, а не просто VIN в остатках.
- Исправления конкретных импортированных договоров по артикулам выполняются вручную пользователем, если это бизнес-корректировка данных.
- Для исправления директорских отчетов по историческим договорам использовать отчетные поля договора `warehouse_id` и `assigned_user_id`, не менять фактический склад прицепа без отдельного запроса.
- Исторические заказы можно создавать задним числом; связку с уже существующим договором делать отдельной проверяемой командой, чтобы не сломать договор/VIN.
- У производственника в левом меню не должно быть нескольких активных пунктов на одном экране.
