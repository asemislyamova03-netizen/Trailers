# NEXT_ACTIONS.md

## Следующие действия

1. Подтянуть изменения на сервер, выполнить миграцию БД и перезапустить приложение.
2. На сервере запустить проверку синтаксиса через рабочий venv: `venv/bin/python -m py_compile models.py views.py migrations/versions/f9a1b2c3d4e5_order_lines_production_foundation.py`.
3. Проверить, что существующие заказы получили строку `CustomerOrderLine` №1, а VIN/потребности/резервы подтянули `order_line_id`.
4. Проверить админскую корректировку VIN: изменить ошибочный свободный VIN и удалить свободную ошибочную запись без связей.
5. Проверить в карточке заказа добавление второй позиции из наличия: VIN должен резервироваться, сумма заказа должна стать суммой строк.
6. Проверить добавление позиции в производство: должна создаваться `SupplyNeed` с `order_line_id`.
7. Проверить создание договора по заказу с несколькими строками: в `sales_contract_line` должны появиться строки, печатная спецификация должна вывести несколько позиций.
8. Добавить редактирование/удаление строк заказа до выдачи документов.
9. Добавить привязку VIN/выпуска производства к конкретной строке заказа из UI.
10. Спроектировать выбор шаблонов договоров по группе/ОТТС/модификации.
11. Спроектировать списание комплектующих и полуфабрикатов по спецификации при выпуске единицы прицепа.
12. Добавить мобильный интерфейс смен сотрудников: открыть смену, выбрать участок, закрыть смену с результатом или без результата.

## Ближайшая техническая задача

- Проверить на сервере новый блок позиций заказа и создание договора со строками.

## Ближайшая бизнес-задача

- Подтвердить новую модель производства: участки, склады комплектующих/полуфабрикатов, сотрудники, смены и результаты месяца для директора.

## Что нельзя забыть

- Не смешивать trailers с clinic app и flexity admin.
- Цены по конфигурации могут быть не заполнены и не должны блокировать заказ.
- Источник выбора параметров конфигуратора - матрица допустимости, а не справочник номенклатуры и не ценовые матрицы.
- Строки заказа добавляются из карточки заказа, но удаление/редактирование и построчная отгрузка ещё не реализованы.
