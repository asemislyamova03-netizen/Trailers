# CURRENT_STATE.md

## Текущее состояние

Дата обновления: 2026-05-29

## Что уже реализовано

- Базовая структура проектного контекста и planning-документов.
- Исправлена обработка конфигуратора при переводе/сохранении заказа в производство через форму редактирования.
- Форма заказа в производство переведена на общую логику конфигуратора: доступные параметры фильтруются по матрице допустимости, ОТТС/VIN показывают ошибки конфигурации явно.
- В справочнике ОТТС/VIN-модификаций номер и модификация ОТТС выбираются из активного справочника ОТТС, а не вводятся свободным текстом.
- В справочнике групп номер ОТТС выбирается из активного справочника ОТТС.
- В подборе прицепа спецпараметры выводятся из матрицы допустимости; ценовые матрицы не скрывают допустимые параметры.
- В подборе прицепа списки параметров динамически обновляются после выбора группы/параметров и синхронизируют hidden-поля действий.
- Конфигуратор больше не подставляет `NOOK` и `NONE` автоматически, если опорное колесо или тент не выбраны.
- Поиск ОТТС/VIN-модификации учитывает выбранную высоту борта для любых типов кузова, включая `GRAINTRAILER`.
- Заказ в производство поддерживает количество больше 1 для одной конфигурации: сумма от конфигуратора умножается на количество, производственная потребность синхронизируется по количеству.
- Добавлена основа для многострочных заказов и договоров: `CustomerOrderLine`, `SalesContractLine`, привязка VIN/резервов/производства к строке заказа.
- Добавлена основа производственного модуля: участки/цеха, сотрудники производства, смены, результаты смен, спецификации номенклатуры, склады комплектующих и полуфабрикатов.
- Менеджерам открыт раздел производства и перемещения; у логиста в навигации оставлен простой логистический контур и реестр VIN.
- Запуск потребностей в производство перенесён к менеджеру: менеджер видит новые потребности своего склада и отдаёт их в производство.
- Логистический контур для роли логиста сужен до реестра VIN; рабочий стол логиста перенаправляет логиста в VIN-реестр, чтобы он не запускал производство, не резервировал/привязывал VIN и не выполнял менеджерские операции.
- Менеджер может привязать VIN к выпущенной единице из своего производственного блока; фактический выпуск изделия остаётся доступен только производственникам.
- Менеджер может отправить готовый прицеп со склада выпуска на свой склад из вкладки готовых к отправке прицепов; действие доступно только если склад назначения совпадает со складом менеджера.
- Реестр VIN для логиста сортируется по порядковому номеру VIN: сверху отображается самый большой `serial7`, а не последняя внесённая запись.
- Менеджеру открыто изменение комплектации прицепа и выпущенной единицы без VIN; пустые опциональные параметры не должны автоматически превращаться в тент/каркас 90 или опорное колесо.
- Проверка изменения комплектации больше не считает смену высоты борта сменой размера рамы/кузова: база сравнивается по группе, коду размера кузова и количеству осей.
- Реестр VIN адаптирован для работы логиста с телефона: кнопка `Открыть` находится рядом с VIN, второстепенные колонки скрываются на малых экранах.
- Администратору добавлена корректировка и удаление ошибочно созданных VIN на начальном этапе.
- В карточке заказа добавлен блок позиций: можно добавить отдельную позицию из наличия с резервом VIN или позицию в производство с созданием потребности; сумма заказа пересчитывается по строкам.
- В карточке заказа добавлено безопасное управление позициями до выдачи документов: редактирование количества/цены/комментария и удаление строки без резервов, VIN, производства, перемещений, договора и других связанных операций.
- Подбор прицепа можно открыть из существующего заказа и добавить другую конфигурацию отдельной строкой: из наличия резервируется выбранный VIN, для производства создаётся новая позиция и `SupplyNeed` по выбранной конфигурации.
- Администратору добавлено безопасное удаление заказа: удаление блокируется, если есть договор, оплаты, резервы, VIN, производство, перемещения, выпуск, документы или отгрузка.
- Администратор больше не направляется на панель директора: стартовая страница и первый пункт меню ведут в `Заказы`; также добавлен вход в `Договоры`.
- При создании договора из заказа создаются строки договора по строкам заказа, а печатная спецификация выводит несколько позиций.
- Выдача документов по заказу проверяет VIN по строкам заказа: для каждой trailer-позиции должно хватать привязанных VIN.
- Физическая отгрузка добавлена по строкам заказа: каждая строка закрывается отдельно, заказ считается отгруженным после отгрузки всех строк.
- Для печати/PDF договора добавлен выбор активного шаблона из `ContractTemplate` по группе, ОТТС, модификации, исполнению и типу клиента; если подходящего файла шаблона нет, используется текущий `contract_print.html`.
- Менеджеру, директору и логисту добавлен раздел `Отчеты`: продажи, динамика, филиалы, типы прицепов, оборачиваемость склада, склад, производство, перемещения, проблемы и финансы.
- Добавлен интерфейс `Шаблоны договоров`: директор/админ может создавать правила выбора шаблона, а в карточке заказа видно, какой шаблон применится к договору.
- Директорские отчеты по продажам/динамике/филиалам/типам/оборачиваемости переведены на агрегированные показатели и графики без портянки заказов.
- Источник продаж в отчетах расширен до `SalesContract`: исторические договоры/продажи, загруженные из Excel без CRM-заказа, учитываются в выручке, количестве, динамике, филиалах, типах и оборачиваемости.
- В отчетах добавлен фильтр направления: все, легковые, грузовые/спец, без VIN/прочее; договоры без VIN/артикула или с суммой от 3 млн ₸ показываются предупреждением как потенциальные искажения.
- В меню производственника убраны дублирующие пункты, которые подсвечивались одновременно; навигация по заданиям осталась вкладками внутри экрана производства.
- Добавлен первый безопасный слой интеграции Kaspi Магазина: импорт заказа Kaspi по коду или списком за период/состояние в заявку `Lead` и входящее сообщение `LeadMessage` без хранения токена в коде.
- Для исторических договоров без CRM-заказа добавляется отчетная атрибуция: склад и ответственный могут храниться прямо в `SalesContract` и использоваться в директорских отчетах без изменения фактического склада прицепа.
- В форме создания/редактирования заказа добавлена дата заказа: можно создавать исторические заказы задним числом через `CustomerOrder.created_at`.
- На сервере для 4 крупных исторических договоров применена отчетная атрибуция: склад `Склад грузовых прицепов`, ответственный `Кальман Игорь Андреевич`.
- Подготовлен первый архитектурный слой под новую модель: категории продукции, мультискладовые продажи менеджера, разделение VIN-ролей, количественный складской учет, складская комплектация, маршруты производства и документ `Реализация товаров и услуг`.
- Менеджерская логика доступа переведена с ограничения по складу на сделки по `assigned_user_id`; склад менеджера остаётся значением по умолчанию, но не должен блокировать продажу с другого склада продаж.
- VIN-процесс разделён по ролям: логист присваивает VIN, менеджер подтверждает физическое нанесение VIN через общий статус `VinRegistry.status = confirmed`.
- Добавлен начальный реестр реализаций товаров и услуг; отгрузка клиенту блокируется, если по прицепу нет проведённой реализации.
- После последней миграции локальная БД обновлена до `b1c2d3e4f5a6`; на сервере пользовательский вывод также показывает `b1c2d3e4f5a6 (head)`.
- Проверены обязательные таблицы новой модели: `product_category`, `inventory_balance`, `inventory_operation`, `trailer_assembly_operation`, `sales_realization`, `customer_order_line`, `sales_contract_line`, `warehouse_storage_area`, `production_workshop`.
- Проверены GET-маршруты под ролями manager/logistics/production/director; все указанные маршруты локально отвечают `200`.
- В списке перемещений кнопки создания и редактирования доступны менеджеру, директору и админу; логист их не видит.
- Меню производственника упрощено до одного пункта `Производство`, вкладки остались внутри экрана производства.
- В VIN-реестре добавлен явный заголовок активного статуса и быстрые фильтры; меню логиста подсвечивает конкретный VIN-фильтр.
- Исправлен риск неверного VIN в `Реализация товаров и услуг`: создание реализации больше не подставляет `order.trailer` в каждую строку заказа, а требует однозначный прицеп/VIN по конкретной строке. Для ошибочного черновика добавлено удаление.
- Отдельная пользовательская отгрузка упразднена: проведение реализации теперь закрывает строки заказа и заказ как отгруженные. Кнопки `Отгрузить` убраны из карточки заказа и рабочего места менеджера; старые POST-маршруты отгрузки перенаправляют к карточке заказа с предупреждением.
- Удаление реализации разрешено для черновика и проведённого документа; при удалении проведённой реализации откатываются статусы отгрузки заказа, строк и прицепов.
- Производственные склады включены как источник готовой техники: прицепы на производственном складе видны менеджеру в наличии, выборе прицепа при создании заказа и в форме перемещения со склада.
- Экран перемещений доработан: добавлены фильтры по типу, статусу, складу отправителя и складу назначения; в одиночном и партийном перемещении показывается явная таблица доступных прицепов со склада отправителя с кнопками выбора/добавления. При статусах `sent/in_transit/arrived` автоматически заполняются даты отправки/прибытия, если пользователь их не указал.
- Итерация 1 начата со стабилизации: создан локальный backup `backups/trailers_before_iteration1_20260529_135604.db`, `flask db current` и `flask db heads` показывают `b1c2d3e4f5a6 (head)`, обязательные таблицы новой модели существуют.
- Основные маршруты под ролями manager/logistics/production/director локально проверены: страницы открываются без 500, `/director` добавлен как редирект на директорскую панель.
- Навигация переведена на явную секцию `navigation_section`: у менеджера, логиста, производственника и директора активируется один пункт меню, а логистский контур оставлен только вокруг VIN-фильтров.
- Менеджерский рабочий экран переименован в `CRM`, в шапке оставлены быстрые действия заказа и заявки, убраны быстрые кнопки подбора и пополнения из главного CRM-заголовка.
- Главный экран менеджера упрощён до CRM-контуров: сделки, диалоги, заявки, задачи/ассистент. Складские, производственные, перемещения, оплаты и отгрузочные таблицы убраны из CRM-экрана и остаются в отдельных разделах.
- VIN-реестр логиста стал понятнее по фильтрам: заголовок и описание меняются по активному статусу, а загрузка VIN скрыта за одной компактной кнопкой `Загрузка VIN`.
- Производственный экран переведён на компактный табличный вид: строки заданий и выпусков сканируются на мобильном без крупных карточек, действия `В работу`, `+1` и комментарий остались внутри строки.
- Редактирование существующего заказа со строками переведено в безопасный режим шапки: POST `/orders/<id>/edit` больше не пересоздаёт строки, не меняет VIN/прицеп, не создаёт/перепривязывает `SupplyNeed` и не трогает резервы.
- В карточке заказа добавлены отдельные действия смены источника обеспечения строки: `production -> stock` отменяет ещё не начатые потребности производства и резервирует выбранный VIN; `stock -> production` снимает резерв VIN/прицепа и создаёт новую `SupplyNeed`.
- Список заказов упрощён под Итерацию 1: вместо технической портянки статусов показывается номер, клиент, менеджер, склад источника, склад выдачи, сумма, один основной статус, главный блокер и следующее действие. Фильтры работают по вычисляемым состояниям сделки.
- GET `/orders/<id>/edit` для заказов со строками теперь открывает отдельную форму `Редактирование шапки заказа`: видны только клиент, менеджер, склад выдачи, даты и комментарии, а позиции показаны справочно без полей VIN/прицепа/источника.
- Новые складские перемещения больше не предлагают тип `Отгрузка клиенту`: клиентская выдача закрывается проведением реализации товаров и услуг. Старые `customer_shipment` остаются доступными в фильтре перемещений как архивный тип.
- Создание реализации строго использует VIN, привязанный к строке заказа. В карточке заказа добавлено явное действие `VIN строки для реализации`: можно привязать активный VIN-реестр к строке или, для legacy-данных, создать связь VIN из физического прицепа строки.
- Перемещение под сделку стало построчным: кнопка `Переместить` появляется у конкретной позиции заказа, если её прицеп находится не на складе выдачи, а `StockMovement` получает `order_line_id`.
- Реализацию в статусах `draft` и `posted` можно редактировать менеджеру своей сделки, директору и админу: дата, цена, количество не-VIN строк и комментарии меняются без смены VIN, прицепа и строки заказа.
- Если по позиции заказа уже есть реализация, исходная строка заказа блокируется от изменения количества, цены и комментария; изменения нужно делать в документе реализации, чтобы заказ и факт продажи не расходились.
- Производственный экран дополнен вкладками Итерации 1: `По складам`, `По размерам рам`, `Мои задания`, `В работе`, `Выпуск`, `История`, `Материалы`. Группировки считаются по активным заданиям без запуска BOM-списаний.
- В меню логиста добавлен отдельный пункт `Загрузка VIN`, который ведёт в тот же VIN-реестр и раскрывает единственную форму загрузки; дублирующих форм загрузки не добавлено.

## Что реализовано частично

- Складская комплектация и реализация товаров заведены на уровне моделей, миграции и первого UI/route-слоя; полный расчет BOM-delta, резервирование/списание комплектующих и возврат при разукомплектации ещё требуют реализации.
- Категории продукции добавлены в конфигуратор, номенклатуру, VIN-реестр, список техники и директорскую панель; бизнес-классификацию существующих групп после миграции нужно проверить на серверных данных.
- Безопасное редактирование заказа реализовано базово: шапка отделена от строк, смена источника обеспечения вынесена в отдельные POST-действия строки. Для строк с количеством больше 1 смена источника пока блокируется, чтобы не создавать скрытые резервы нескольких VIN в одной операции.

## Что не реализовано

- Полный экран комплектации с расчетом `текущий BOM -> целевой BOM`, проверкой `InventoryBalance`, дефицитами, резервом, списанием и возвратом комплектующих.
- Проведение реализации пока не списывает отдельно продаваемые комплектующие из `InventoryBalance` и не формирует экспорт 1С/ЭСФ.
- Производственный выпуск по кнопке `Выпущено 1 шт.` ещё не списывает комплектующие по спецификации.

## Что требует проверки

- Перезапустить серверный systemd-сервис с реальным именем сервиса после выкладки шаблонных правок.
- Создание или редактирование заказа с новой конфигурацией прицепа и источником обеспечения `production`, включая сценарий без заполненной цены.
- Заполнение строк ОТТС/VIN-модификаций через выбор действующего ОТТС из списка.
- Выбор группы `006G` на сервере: если в матрице допустимости стоят флажки для высоты борта и SINGLE/DOUBLE, эти параметры должны отображаться в заказе и подборе.
- Проверить, что для грузовых групп без флажка опорного колеса поле опорного колеса остаётся пустым/без вариантов.
- Проверить на сервере `006G + GRAINTRAILER + E150 + DOUBLE`: строка ОТТС есть, после обновления должна подтянуть VIN-модификация `000011`.
- Проверить создание/редактирование производственного заказа с количеством 2 и одной конфигурацией.
- Проверить добавление двух разных конфигураций в один заказ через карточку заказа → подбор прицепа.
- Проверить удаление админом пустого тестового заказа и блокировку удаления заказа с резервом/VIN/оплатой/договором.
- Проверить, что менеджер видит новые потребности и может отдать их в производство, а логист больше не видит этот блок.
- Проверить, что менеджер может открыть выпущенную единицу без VIN и привязать VIN, а производственные кнопки выпуска для менеджера неактивны.
- Проверить, что менеджер видит кнопку `Переместить на мой склад` для готового прицепа со своим складом назначения и после нажатия получает входящее перемещение.
- Проверить в реестре VIN у логиста, что сверху показывается последний VIN по порядковому номеру, а не по дате внесения.
- Проверить на сервере изменение комплектации у менеджера: смена борта, тента и опорного колеса не должна менять группу/кузов/размер рамы и не должна подставлять `90` при пустом тенте.
- Проверить производство и реестр VIN на смартфоне после серверного обновления; локальная browser-проверка в Codex не запустилась из-за ошибки доступа runtime.
- Проверить раздел `Отчеты` под ролями manager/director/logistics на серверных данных: продажи, динамика, филиалы, типы прицепов и оборачиваемость склада.
- Проверить, что исторические Excel-продажи есть именно в `SalesContract`; если Excel загрузил только VIN/остатки без договора, для выручки нужны дата продажи и сумма.
- Проверить выбор шаблона договора после заполнения `contract_template`: fallback должен оставлять текущий печатный договор рабочим.
- Проверить на сервере, что у производственника слева активен только один пункт `Производство`.
- Интеграция Kaspi требует проверки API/формата данных и безопасного хранения токена; токен нельзя хранить в коде.
- Проверить на сервере переменную окружения `KASPI_SHOP_TOKEN` и импорт реального заказа Kaspi через раздел `Kaspi`.
- Артикулы Kaspi могут отличаться от внутренней номенклатуры; сейчас точное совпадение используется только как подсказка, а полноценный справочник соответствий ещё не реализован.
- Проверить на сервере корректировку исторических крупных договоров: суммы 18,8 млн, 18 млн, 14 млн и 8,5 млн должны попадать в директорских отчетах на склад грузовых прицепов и ответственного Кальмана Игоря.
- Проверить создание заказа задним числом и последующую связку исторического договора с таким заказом отдельной командой.
- Проверить `ORD-000027` отдельно как продажу комплекта запасных частей; сейчас его не нужно переводить на склад грузовых прицепов как прицеп.
- После миграции проверить, что меню менеджера, логиста, производственника и директора соответствует новой ролевой логике.
- Проверить создание и проведение реализации по заказу до физической отгрузки.
- Проверить, что отгрузка без проведённой реализации блокируется понятным сообщением.
- Проверить на сервере заказ, где ранее создался черновик реализации с чужим VIN: удалить черновик, убедиться, что новая реализация создаётся только с VIN из строки заказа или блокируется понятной ошибкой.
- Проверить новый сценарий: `Создать реализацию` -> `Провести и закрыть отгрузку`, без отдельной кнопки отгрузки.
- Проверить, что готовый прицеп на производственном складе отображается в `Наличие`, в выборе VIN при создании заказа и в перемещении с производственного склада на склад менеджера.
- Проверить форму одиночного и партийного перемещения: после выбора склада отправителя список доступных VIN должен обновляться без ручного ввода поиска.
- Проверить на сервере список `/orders`: фильтры по состояниям сделки, главный блокер и следующее действие должны совпадать с реальным состоянием заказов.
- Проверить на сервере `/orders/<id>/edit` для заказа со строками: форма должна быть шапкой заказа без выбора VIN/прицепа/источника, а сохранение не должно менять строки, резервы и `SupplyNeed`.
- Проверить на сервере `/stock-movements/new`: в типе перемещения должны быть только `Между складами` и `Поступление с производства`, без новой `Отгрузки клиенту`.
- Проверить на сервере проблемный заказ вроде `ORD-000035`: в позиции заказа должна появиться кнопка привязки/создания связи VIN, после неё реализация должна создаваться с VIN именно этой строки.
- Проверить построчное перемещение в карточке заказа: у позиции с VIN на другом складе кнопка `Переместить` должна создать движение с правильным `order_line_id`, не меняя другие позиции заказа.
- Проверить редактирование проведённой реализации: кнопка `Редактировать` должна быть доступна, VIN-строки не должны позволять менять количество/VIN, а сумма документа должна пересчитываться.
- Проверить, что после создания реализации поля строки заказа становятся readonly и показывают подсказку редактировать документ реализации.
- Проверить `/production/workspace` на сервере и телефоне: вкладки по складам, размерам рам, история и материалы должны открываться без 500 и оставаться компактными.
- Проверить `/logistics/vin-registry?upload=1`: у логиста должен быть активен пункт `Загрузка VIN`, форма должна быть раскрыта, остальные VIN-фильтры не должны подсвечиваться одновременно.
- Проверить на смартфоне `/production/workspace`: строки заданий должны быть компактными, без больших карточек.
- Проверить `/logistics/vin-registry`: активный фильтр должен быть понятен по заголовку, а загрузка VIN должна открываться только по кнопке.
- Проверить категории `light_trailer` / `cargo_vehicle` у существующих групп и номенклатуры, особенно G-группы и зерновозы.

## Главный фокус сейчас

- Стабилизировать новую модель продаж: сделка менеджера, склад источника, склад выдачи, VIN-процесс, складская комплектация, реализация товаров и услуг как источник выручки.

## Риски

- Не смешать бизнес-логику trailers с другими проектами.
- При неверной обработке конфигуратора заказ может сохраниться без ОТТС/VIN-модификации, что ломает дальнейшую VIN-логику.
- Незаведённые цены допустимы, поэтому отсутствие цены должно оставаться предупреждением, а не блокировкой.
- Полный пользовательский сценарий двух разных прицепов в одном заказе реализован частично: добавление, безопасное редактирование/удаление строк и построчная отгрузка есть, но ещё нужно проверить привязку VIN к производственной строке на серверных данных.
- Автоматическое списание комплектующих/полуфабрикатов по спецификации ещё не включено в бизнес-операции выпуска.
- Текущий сервер/IP не подходит для проверки Kaspi API: `google.com` отвечает, а `kaspi.kz` уходит в timeout. Нужна проверка на хостинге организации или другом IP.
- Запчасти и комплекты без VIN пока не имеют отдельного жизненного цикла; ручные правки таких заказов через прицепную складскую логику могут исказить учет.
- Миграция добавляет много новых таблиц и полей; перед серверным обновлением нужен backup базы.
- После включения блокировки отгрузки без реализации пользователям нужно сначала создавать и проводить реализацию по сделке.
